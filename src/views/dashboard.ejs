<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0", viewport-fit=flexible>
    <title>Dashboard - Sistema Académico</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: #f5f5f5;
        }
        .dashboard {
            max-width: 800px;
            margin: 50px auto;
            padding: 30px;
            background: white;
            border-radius: 15px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
            text-align: center;
        }
        .user-info {
            margin-bottom: 30px;
        }
        .user-photo {
            width: 80px;
            height: 80px;
            border-radius: 50%;
            margin-bottom: 15px;
        }
        .logout-btn {
            padding: 10px 20px;
            background: #e74c3c;
            color: white;
            border: none;
            border-radius: 8px;
            text-decoration: none;
            font-weight: 600;
        }
        .logout-btn:hover {
            background: #c0392b;
        }
    </style>
</head>
<body>
    <div class="dashboard">
        <div class="user-info">
            <% if (user.provider === 'google') { %>
                <img src="<%= user.photo %>" alt="Foto" class="user-photo">
            <% } %>
            <h1>¡Bienvenido, <%= user.name %>!</h1>
            <p><%= user.email %></p>
            <p><strong>Proveedor:</strong> <%= user.provider || 'Sistema' %></p>
        </div>
        <p>Has iniciado sesión exitosamente en el Sistema Académico CEO.</p>
        <br>
        <a href="/logout" class="logout-btn">Cerrar Sesión</a>
    </div>
</body>
</html>