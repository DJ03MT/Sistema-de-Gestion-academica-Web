<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profesores Inactivos</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/styles/menusecretaria.css">
    <style>
        /* Estilos para que se vea como tus otras páginas */
        body { background: #f8f9fa; padding: 20px; }
        .form-container { background: white; padding: 30px; border-radius: 16px; max-width: 1000px; margin: 20px auto; }
        .form-actions { margin-top: 20px; }
        .btn-reactivate { background: rgba(3, 130, 172, 0.15); color: var(--primary); }
        .btn-reactivate:hover { background: var(--primary); color: white; }
    </style>
</head>
<body>
    <div class="form-container">
        <h1><i class="fas fa-archive"></i> Archivo de Profesores Inactivos</h1>
        <p>Esta lista muestra a los profesores cuyas cuentas han sido desactivadas.</p>

        <div class="table-container" style="margin-top: 20px;">
            <table class="data-table">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Nombre Completo</th>
                        <th>Cédula</th>
                        <th>Acción</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (profesores.length > 0) { %>
                        <% profesores.forEach(profe => { %>
                            <tr>
                                <td>PROF-<%= profe.ID_PROFESOR %></td>
                                <td><%= profe.PRIMER_APELLIDO %>, <%= profe.PRIMER_NOMBRE %></td>
                                <td><%= profe.CEDULA_IDENTIDAD || 'N/A' %></td>
                                <td>
                                    <form action="/secretaria/profesores/<%= profe.ID_PROFESOR %>/reactivar" method="POST">
                                        <button type="submit" class="btn-action btn-reactivate">
                                            <i class="fas fa-undo"></i> Reactivar
                                        </button>
                                    </form>
                                </td>
                            </tr>
                        <% }); %>
                    <% } else { %>
                        <tr>
                            <td colspan="4" style="text-align: center; padding: 20px;">
                                No hay profesores inactivos.
                            </td>
                        </tr>
                    <% } %>
                </tbody>
            </table>
        </div>

        <div class="form-actions">
            <a href="/secretaria" class="btn" style="background: #ccc; text-decoration: none; color: black;">
                <i class="fas fa-arrow-left"></i> Volver al Dashboard
            </a>
        </div>
    </div>
</body>
</html>